import { IPaginatable } from '@/application/support/IPaginatable';
import { ISortable } from '@/application/support/ISortable';

export interface IArticleListCriteria extends IPaginatable, ISortable {

}
